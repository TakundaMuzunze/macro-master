<script module>
	import { defineMeta } from '@storybook/addon-svelte-csf';
	import SwapIcon from './SwapIcon.svelte';

	const { Story } = defineMeta({
		component: SwapIcon,
		tags: ['autodocs'],
		title: 'Icons/Swap Icon'
	});
</script>

<Story name="Smart Swap Icon" />
