<script module>
	import { defineMeta } from '@storybook/addon-svelte-csf';
	import ShareIcon from './ShareIcon.svelte';

	const { Story } = defineMeta({
		component: ShareIcon,
		tags: ['autodocs'],
		title: 'Icons/Share Icon'
	});
</script>

<Story name="Share Recipes Icon " />
